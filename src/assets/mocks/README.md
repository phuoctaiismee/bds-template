# Mock assets

This folder keeps mock assets used in development.

## Variable naming

We use `mock` prefix for mock assets to differentiate them from real assets.

### Example

```ts
import { mockLive1Image } from './images/live-1.png';
```
