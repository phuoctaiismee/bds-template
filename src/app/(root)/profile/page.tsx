import ProfileScreen from '@/modules/screens/profile';
import { FC } from 'react';

type ProfilePageProps = object;
const ProfilePage: FC<ProfilePageProps> = () => {
  return <ProfileScreen />;
};

export default ProfilePage;
