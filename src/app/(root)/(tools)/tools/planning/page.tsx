import PlanningScreen from '@/modules/screens/(tools)/planning';
import { FC } from 'react';

type PlanningPageProps = {};
const PlanningPage: FC<PlanningPageProps> = () => {
  return <PlanningScreen />;
};

export default PlanningPage;
