import NewsScreen from '@/modules/screens/news';
import { FC } from 'react';

type NewPageProps = object;
const NewsPage: FC<NewPageProps> = () => {
  return <NewsScreen />;
};

export default NewsPage;
