import HomeScreen from '@/modules/screens/home';
import { FC } from 'react';

type FrontHomePageProps = object;

const FrontHomePage: FC<FrontHomePageProps> = () => {
  return <HomeScreen />;
};

export default FrontHomePage;
