import MessagesScreen from '@/modules/screens/messages';
import { FC } from 'react';

type MessagePageProps = object;
const MessagesPage: FC<MessagePageProps> = () => {
  return <MessagesScreen />;
};

export default MessagesPage;
